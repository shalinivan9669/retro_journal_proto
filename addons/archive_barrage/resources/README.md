# Prebaked barrage resources

The `.res` terrain meshes and collision shapes in this folder are generated by
`res://addons/archive_barrage/tools/bake_barrage_terrain.gd`. They remove the
large synchronous mesh-generation hitch when the level is opened through the
project scene loader.
